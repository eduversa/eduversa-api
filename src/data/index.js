const OPTIONS = require("./options.data");
const REGEX = require("./regex.data");
const ROLES = require("./roles.data");
const HTTP_STATUS = require("./statuscodes.data");

module.exports = { HTTP_STATUS, ROLES, REGEX, OPTIONS };
